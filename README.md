# Combius
## An Multi-Token owo bot with safety 

Multi-token OWO Discord selfbot with gems, inventory, sellall, and captcha handling.

## Quick Start

### 1. Clone & Install

```bash
git clone <your-repo>
cd owo-selfbot
pip install -r requirements.txt
---

## 🚀 **How to Deploy on Railway**

### Step 1: Push to GitHub
```bash
# The .env is in .gitignore - safe to push
git init
git add .
git commit -m "Initial commit"
git clone this repo
git push -u origin main